import { ValidatorCharacterIsNumberDirective } from './validator-character-is-number.directive';

describe('ValidatorCharacterIsNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatorCharacterIsNumberDirective();
    expect(directive).toBeTruthy();
  });
});
